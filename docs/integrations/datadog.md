# datadog Integration

Configure in AgentShield Admin → Integrations.

## Setup
1. Generate webhook/token in the service
2. Add in AgentShield with your credentials
3. Test with "Send Test Event"

## Alert Routing
Alerts with severity >= configured threshold are dispatched automatically.
