import React from 'react';

interface FindingsListProps {
  [key: string]: unknown;
}

const FindingsList: React.FC<FindingsListProps> = (props) => {
  return (
    <div className="FindingsList-container">
      {/* FindingsList component */}
    </div>
  );
};

export default FindingsList;
